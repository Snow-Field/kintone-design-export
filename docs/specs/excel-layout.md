# Excel設計書のレイアウト仕様

出力される .xlsx の構成・体裁を定める。判断の理由は `docs/ai/decisions/excel-output.md` を参照。

実装の進捗と未確認の事項は `docs/status.md` を参照。

## 目次

- [シート構成](#シート構成)
- [共通の体裁](#共通の体裁)
- [配色](#配色)
- [フィールドシートの列構成](#フィールドシートの列構成)
- [紐づけ情報の表現](#紐づけ情報の表現)
- [値の表記](#値の表記)

## シート構成

| # | シート | 内容 |
| --- | --- | --- |
| 1 | 一般情報 | ドメイン・アプリ名・アプリID・説明 |
| 2 | フィールド | 全フィールドの属性一覧 |
| 3 | アクション情報 | アクションの設定とフィールドの関連付け |
| 4 | ルックアップ情報 | ルックアップの設定とフィールドのコピー元・コピー先 |
| 5 | 関連レコード情報 | 関連レコード一覧の設定と表示フィールド |
| 6 | 一覧 | 一覧の設定と表示フィールド |
| 7 | アプリのアクセス権 | ユーザー・グループ・組織ごとの権限 |
| 8 | レコードのアクセス権 | 条件ごとの権限 |
| 9 | フィールドのアクセス権 | フィールドごとの権限 |
| 10 | プロセス管理 | ステータスと作業者、アクションと遷移条件 |

シートの並び順は上表のとおり。

## 共通の体裁

- 表は A1 から始める。余白の行や列は設けない。
- 見出し行を固定する。2段見出しのシートは2行とも固定する。
- 表が1つだけのシートには見出し行にオートフィルタを設定する。Excel の仕様上シートあたり1つまでのため、表が複数あるシート（プロセス管理）には設定しない。
- 列幅は見出しと値の表示幅（全角2・半角1）から算出する。折り返し表示は使わない。
- 偶数行に薄い背景色を敷く。
- 権限の有無は `■`（あり）と `□`（なし）で表す。

## 配色

色とフォントの定義は1か所に集約し、そこを差し替えれば全シートに反映される。

| 用途 | 色 | 文字色 | コントラスト比 | 判定 |
| --- | --- | --- | --- | --- |
| 上段見出し | `#1F3864` | `#FFFFFF` | 11.6:1 | WCAG AAA |
| 下段見出し | `#2F5597` | `#FFFFFF` | 7.3:1 | WCAG AAA |
| 偶数行の背景 | `#F2F7FC` | 既定（黒） | — | — |
| 罫線 | `#BFCBD9` | — | — | — |

フォントは Yu Gothic UI。

色を変更する場合もコントラスト比 4.5:1（WCAG AA）以上を維持する。

## フィールドシートの列構成

上段の見出しでグループ化し、下段に個々の列を置く。

| グループ | 列 |
| --- | --- |
| 基本 | No.／フィールドコード／フィールド名／タイプ／テーブル／グループ |
| 入力制約 | 必須／重複禁止／最大長／最小長／最大値／最小値 |
| 初期値・選択肢 | デフォルト値／選択肢／選択肢の並び／対象ユーザー |
| 計算・表示形式 | 計算式／計算式の表示／表示形式／小数桁／単位／単位の位置／桁区切り |
| 表示 | ラベル表示／リンク種別／サムネイルサイズ／グループ初期表示／機能の有効 |

1フィールドを1行で表す。サブテーブル内のフィールドも行として出力し、「テーブル」列に明細名を入れる。グループとサブテーブルはそれ自体の見出し行も出力する。

## 紐づけ情報の表現

フィールド同士の対応関係は1組を1行として展開し、対応元と対応先を別の列に置く。

対象は次のとおり。

| シート | 対象 |
| --- | --- |
| ルックアップ情報 | ほかのフィールドのコピー、表示フィールド |
| アクション情報 | フィールドの関連付け |
| 関連レコード情報 | 表示フィールド |
| 一覧 | 表示フィールド |

単一のフィールドが複数の値を持つだけの属性（選択肢、対象ユーザーなど）は行を分けず、列の中でカンマ区切りにする。

## 値の表記

kintone REST API が返す値をそのまま出力し、日本語へ変換しない。フィールド型（`SINGLE_LINE_TEXT` など）、エンティティ種別（`USER` / `GROUP` / `ORGANIZATION`）、単位の位置（`BEFORE` / `AFTER`）、作業者の指定方式（`ONE` / `ALL` / `ANY`）が該当する。
